(function () {
  if ( window.Pipes === undefined ) {
    window.Pipes = {};
  }

  var Board = Pipes.Board = function Board() {}

  // take a size!
  Board.generateBoard = function () {
    
  };

})();
